# .pre-commit-config.yaml
repos:
  - repo: local
    hooks:
      - id: isort
        name: isort
        entry: isort
        language: system
        types: [python]
        stages: [commit]
      - id: black
        name: black
        language: system
        entry: black
        types: [python]
        stages: [commit]
      - id: ruff
        name: ruff
        language: system
        entry: ruff
        types: [python]
        stages: [commit]
